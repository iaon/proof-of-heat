- id: deploy
  execute-command: /bin/sh
  command-working-directory: /home/iaon/git/proof-of-heat
  pass-arguments-to-command:
    - source: string
      name: -c
    - source: string
      name: git pull && docker compose up --build -d app
