services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./conf:/app/conf
      - ./proof_of_heat:/app/proof_of_heat
    environment:
      - PYTHONUNBUFFERED=1
    command: ["python", "-m", "proof_of_heat.main"]
