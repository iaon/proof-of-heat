version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    command: ["uvicorn", "proof_of_heat.main:app", "--host", "0.0.0.0", "--port", "8000"]
